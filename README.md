# 🚀 Bot WhatsApp – Teste Vocacional

Este projeto implementa um **bot de WhatsApp** utilizando `whatsapp-web.js`, que realiza um **teste vocacional interativo** baseado em uma árvore de decisão.  
O bot envia perguntas ao usuário autorizado e, ao final, recomenda uma área profissional baseada nas respostas.

---

## 📦 Instalação das Dependências

Para instalar as dependências necessárias, execute:

```bash
npm init -y
npm install whatsapp-web.js qrcode-terminal
